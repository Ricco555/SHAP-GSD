__pycache__/
visualisation/
artifacts/
feature_store/
graphs/
node_state_snapshots/
outputs/
runs/
specs/

# Third-party baseline-explainer checkouts (Phase 10) — cloned per README,
# never committed
/external/

# Dataset directories and large data files — never commit raw data to Git
/data/
*.csv
*.parquet
datasets/

# Exception: owner-authored proxy-GT mapping CSVs, hand-written citation
# tables (not pipeline output/raw data) released as supplementary material
!/explore/evaluation/data/*.csv

# instructions and guides
CLAUDE.md
CHANGELOG.md
AGENT.md

# local-only notes, plans, and guides (never committed)
/local/

# dashboard — node_modules only; source is committed
dashboard/node_modules/
dashboard/.vite/

# environment secrets
.env
.env.*
